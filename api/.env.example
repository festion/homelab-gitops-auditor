# Infisical Configuration - Application Secrets (homelab-gitops project)
# Service token for accessing application secrets from Infisical
INFISICAL_TOKEN=st.your-service-token-here
INFISICAL_SITE_URL=https://infisical.internal.lakehouse.wtf
INFISICAL_PROJECT_ID=your-project-id-here
INFISICAL_ENVIRONMENT=dev

# Infisical Admin Configuration - Infrastructure Secrets (homelab-admin project)
# Service token for accessing infrastructure/DevOps secrets
INFISICAL_ADMIN_TOKEN=st.your-admin-token-here
INFISICAL_ADMIN_ENV=prod

# Application Secrets (to be migrated to Infisical)
# These will be fetched from Infisical when INFISICAL_TOKEN is set

# WikiJS Integration
# WIKIJS_URL=https://wiki.internal.lakehouse.wtf
# WIKIJS_TOKEN=your-wikijs-token

# GitHub Integration
# GITHUB_TOKEN=your-github-token
# GITHUB_WEBHOOK_SECRET=your-webhook-secret

# Database
# DB_PATH=./data/gitops.db

# Authentication
# JWT_SECRET=your-jwt-secret
# DEFAULT_ADMIN_PASSWORD=change-me-on-first-login

# API Configuration
# PORT=3070
# NODE_ENV=production

# Email Notifications (if configured)
# SMTP_HOST=smtp.example.com
# SMTP_PORT=587
# SMTP_USER=notifications@example.com
# SMTP_PASSWORD=your-smtp-password
# EMAIL_FROM=gitops-auditor@example.com

# AdGuard Integration (if configured)
# ADGUARD_URL=http://adguard.local
# ADGUARD_USERNAME=admin
# ADGUARD_PASSWORD=your-adguard-password
